import { PageBanner } from '../components'

export default function Meetings() {
  return (
    <div className="page">
      <PageBanner title="Meetings" subtitle="Scheduled interviews" />
      <p className="page__placeholder">Day-grouped meetings, Join → room (BL-FE-10).</p>
    </div>
  )
}
