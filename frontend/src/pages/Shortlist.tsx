import { PageBanner } from '../components'

export default function Shortlist() {
  return (
    <div className="page">
      <PageBanner title="Shortlist" subtitle="Ranked candidates" />
      <p className="page__placeholder">Ranked table + re-rank (BL-FE-07).</p>
    </div>
  )
}
