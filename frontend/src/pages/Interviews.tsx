import { PageBanner } from '../components'

export default function Interviews() {
  return (
    <div className="page">
      <PageBanner title="Interviews" subtitle="AI screening calls" />
      <p className="page__placeholder">Call ops: stats, log, retry queue (BL-FE-09).</p>
    </div>
  )
}
